# Example: MCP client

```bash
# Terminal A
screenlane mcp

# Or configure in your MCP host:
# command: npx
# args: ["-y", "@talocode/screenlane", "mcp"]
```

Call `tools/list` then `tools/call` with `screenlane_demo`.
